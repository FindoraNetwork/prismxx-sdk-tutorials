export function getERC20Contract() {
  // const contract = new ethers.Contract(ysUsdt, MintAbi, wallet);
  // const transaction = await contract.functions.mint(item.address, amount);
  // const tokenResult = await transaction.wait();
}
